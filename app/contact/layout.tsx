export const metadata = {
  title: "Contact Us - MB Decor",
  description: "Get in touch with MB Decor for your decoration needs",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
